/**
 * @deprecated This file is being refactored. Please import from individual service files or from the services index.
 *
 * Example:
 * ```ts
 * // Instead of
 * import { authService } from '../services/api';
 *
 * // Use
 * import { authService } from '../services';
 * ```
 */

// Re-export all services from the new structure
export * from "./index";
